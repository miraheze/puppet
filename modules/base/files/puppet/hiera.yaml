---
:backends: yaml
:yaml:
  :datadir: /etc/puppet/hieradata
:private:
  :datadir: /root/private/hieradata
:hierarchy:
  - "private/%{::hostname}"
  - "hosts/%{::hostname}"
  - "private/common"
  - "common"
