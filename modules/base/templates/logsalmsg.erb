#!/bin/bash

if [ $USER = 'root' ]
then
  ACTOR=$SUDO_USER
else
  ACTOR=$USER
fi

<%- if @use_new_mon -%>
echo '!log' "[$ACTOR@$HOSTNAME] $*" | nc -q0 -u mon111.miraheze.org 5071
<%- else -%>
echo '!log' "[$ACTOR@$HOSTNAME] $*" | nc -q0 -w0 -u mon2.miraheze.org 5071
<%- end -%>
