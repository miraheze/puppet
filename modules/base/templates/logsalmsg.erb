#!/bin/bash

if [ $USER = 'root' ]
then
  ACTOR=$SUDO_USER
else
  ACTOR=$USER
fi

<%- if @use_new_mon -%>
# mon111.miraheze.org
echo '!log' "[$ACTOR@$HOSTNAME] $*" | nc -q0 -u 2a10:6740::6:205 5071
<%- else -%>
echo '!log' "[$ACTOR@$HOSTNAME] $*" | nc -q0 -u mon2.miraheze.org 5071
<%- end -%>
