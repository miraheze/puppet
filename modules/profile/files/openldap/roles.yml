admin:
    display_name: Administrators
    description: Super administrators of the system
    LC_admins: True
    backends_groups:
        ldap:
            - cn=sre,ou=groups,dc=miraheze,dc=org

users:
    display_name: Users
    description: Basic users of the system
    backends_groups:
        ldap:
            - cn=users,ou=group,dc=miraheze,dc=org
