<%-
hosts = [];

@servers.each do |server|
  target = "#{server}:#{@port}"
  hosts.push ( target )
end
-%>
# File managed by puppet
<%= scope.call_function( 'ordered_yaml', [{ 'targets' => hosts }] ) %>
