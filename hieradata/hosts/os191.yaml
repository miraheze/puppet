http_proxy: 'http://bastion.fsslc.wtnet:8080'

role::opensearch::roles: ['data', 'ingest']
role::opensearch::discovery_host:
  - os151.fsslc.wtnet
  - os161.fsslc.wtnet
  - os191.fsslc.wtnet
  - os201.fsslc.wtnet
role::opensearch::manager_hosts:
  - os151

base::syslog::rsyslog_udp_localhost: true
