users::groups: 'cache-admins'
varnish::cache_file_size: '70G'
varnish::thread_pool_max: 12000
nginx::worker_processes: 6

base::syslog::rsyslog_udp_localhost: true
nginx::logrotate_number: 2

cp_use_ipsec: true

cache::nodes:
  lu:
   - 'cp27.wikitide.net'
  usa:
   - 'test151.wikitide.net'
