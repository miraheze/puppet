# should be ssl and php7 and rewrite but piwik adds it
# so this causes a dependancy error
ganglia::modules: ['rewrite', 'ssl', 'php7.0']
grafana::modules: ['headers', 'proxy', 'proxy_http', 'php7.0', 'rewrite', 'ssl']
piwik::modules: ['expires']
puppetmaster_version: 4
